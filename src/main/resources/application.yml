server:
  port: 9090
  tomcat:
    uri-encoding: utf-8
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/spring?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=Asia/Shanghai
    hikari:
      username: root
      password: root

mybatis:
  mapper-locations: classpath:/mapper/*
  type-aliases-package: com.springboot.demo01.pojo
